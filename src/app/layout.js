import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "PDF Crop Tool - Crop PDFs and Remove Pages",
  description: "Free online tool to crop PDF files and remove unwanted pages. Easy to use with no installation required.",
  keywords: "pdf crop, pdf editor, remove pdf pages, crop pdf online, pdf tool",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
